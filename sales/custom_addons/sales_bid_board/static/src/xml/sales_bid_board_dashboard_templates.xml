<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="sales_bid_board.AnalyticsDashboardClient">
        <div class="o_sales_bid_dashboard p-3">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <h2 class="mb-1"><i class="fa fa-line-chart me-2"/>Sales Analytics Dashboard</h2>
                    <p class="text-muted mb-0">Bid board operational overview</p>
                </div>
                <div>
                    <button class="btn btn-success me-2" t-on-click="exportPDF" t-att-disabled="state.loading">
                        <i class="fa fa-file-pdf-o me-1"/>Export PDF
                    </button>
                    <button class="btn btn-primary" t-on-click="loadData" t-att-disabled="state.loading">
                        <i class="fa fa-refresh me-1" t-att-class="{'fa-spin': state.loading}"/>Refresh
                    </button>
                </div>
            </div>

            <div class="card mb-3">
                <div class="card-header"><i class="fa fa-filter me-2"/>Filters</div>
                <div class="card-body">
                    <div class="row g-2">
                        <div class="col-md-2">
                            <label class="form-label">From</label>
                            <input type="date" class="form-control form-control-sm" t-att-value="state.filters.date_from"
                                   t-on-change="(ev) => this.onDateChange('date_from', ev.target.value)"/>
                        </div>
                        <div class="col-md-2">
                            <label class="form-label">To</label>
                            <input type="date" class="form-control form-control-sm" t-att-value="state.filters.date_to"
                                   t-on-change="(ev) => this.onDateChange('date_to', ev.target.value)"/>
                        </div>
                        <div class="col-md-2">
                            <label class="form-label">State</label>
                            <select class="form-select form-select-sm" t-on-change="(ev) => this.onFilterChange('state', ev.target.value)">
                                <option value="">All</option>
                                <t t-foreach="state.filterOptions.states" t-as="opt" t-key="opt.value">
                                    <option t-att-value="opt.value" t-att-selected="state.filters.state === opt.value"><t t-esc="opt.label"/></option>
                                </t>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Review Status</label>
                            <select class="form-select form-select-sm" t-on-change="(ev) => this.onFilterChange('review_status', ev.target.value)">
                                <option value="">All</option>
                                <t t-foreach="state.filterOptions.review_statuses" t-as="opt" t-key="opt.value">
                                    <option t-att-value="opt.value" t-att-selected="state.filters.review_status === opt.value"><t t-esc="opt.label"/></option>
                                </t>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Decision</label>
                            <select class="form-select form-select-sm" t-on-change="(ev) => this.onFilterChange('decision_final', ev.target.value)">
                                <option value="">All</option>
                                <t t-foreach="state.filterOptions.decisions" t-as="opt" t-key="opt.value">
                                    <option t-att-value="opt.value" t-att-selected="state.filters.decision_final === opt.value"><t t-esc="opt.label"/></option>
                                </t>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div t-if="state.loading" class="text-center py-5">
                <i class="fa fa-spinner fa-spin fa-3x text-primary"/>
            </div>
            <div t-if="state.error and !state.loading" class="alert alert-danger"><t t-esc="state.error"/></div>

            <div t-if="!state.loading and !state.error">
                <div class="row g-3 mb-3">
                    <t t-foreach="state.kpis" t-as="kpi" t-key="kpi.name">
                        <div class="col-lg-2 col-md-4 col-sm-6">
                            <div class="card h-100 shadow-sm" t-att-class="kpi.action ? 'cursor-pointer' : ''"
                                 t-on-click="() => kpi.action ? this.onKPIClick(kpi.action) : undefined">
                                <div class="card-body">
                                    <div class="small text-muted"><t t-esc="kpi.name"/></div>
                                    <div class="h4 mb-0"><t t-esc="kpi.value"/><t t-esc="kpi.suffix || ''"/></div>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>

                <div class="row g-3 mb-3">
                    <t t-foreach="state.charts" t-as="chart" t-key="chart.title">
                        <div class="col-lg-6">
                            <div class="card shadow-sm">
                                <div class="card-header"><t t-esc="chart.title"/></div>
                                <div class="card-body">
                                    <div style="position: relative; height: 280px;">
                                        <canvas t-attf-id="sbb_chart_{{chart_index}}"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>

                <div class="row g-3">
                    <t t-foreach="state.tables" t-as="table" t-key="table.title">
                        <div class="col-12">
                            <div class="card shadow-sm">
                                <div class="card-header"><t t-esc="table.title"/></div>
                                <div class="card-body p-0">
                                    <div class="table-responsive">
                                        <table class="table table-sm table-hover mb-0">
                                            <thead class="table-light">
                                                <tr>
                                                    <t t-foreach="table.columns" t-as="col" t-key="col">
                                                        <th><t t-esc="col"/></th>
                                                    </t>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <t t-foreach="table.rows" t-as="row" t-key="row.id">
                                                    <tr t-att-class="table.res_model ? 'cursor-pointer' : ''"
                                                        t-on-click="() => this.onRowClick(table, row)">
                                                        <t t-foreach="row.data" t-as="cell" t-key="cell_index">
                                                            <td><t t-esc="cell"/></td>
                                                        </t>
                                                    </tr>
                                                </t>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
            </div>
        </div>
    </t>
</templates>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_attendance_odoo_sync_outbox" model="ir.cron">
        <field name="name">Berkeley Workforce: process Odoo → Odoo replication queue</field>
        <field name="interval_number">5</field>
        <field name="interval_type">minutes</field>
        <field name="active" eval="True"/>
        <field name="user_id" ref="base.user_root"/>
        <field name="model_id" ref="attendance_core.model_attendance_odoo_sync_outbox"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
env['attendance.odoo.sync.outbox'].sudo().cron_process_pending()
        ]]></field>
    </record>
</odoo>

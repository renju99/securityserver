# Database (required for API and seed). Matches docker-compose db when using Docker.
DATABASE_URL=postgres://admin:password123@localhost:5432/cleaner_attendance

# JWT for auth (required for API)
JWT_SECRET=your-secret-key-change-in-production

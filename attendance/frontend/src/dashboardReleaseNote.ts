/** Bump id when you change the copy so users see the note again. */
export const DASHBOARD_RELEASE_NOTE_ID = 'wf360-ux-20260203';

export const DASHBOARD_RELEASE_NOTE_TITLE = 'What changed';

export const DASHBOARD_RELEASE_NOTE_ITEMS: string[] = [
    'Staff mobile: clearer next check-in or check-out, status sync time, and plain-language errors.',
    'Supervisors: “Today” pulse on the dashboard, geo alerts grouped for today, and open on Live Map from an alert.',
    'HR: conflict screen suggests merge vs void; attendance rows can show an audit history.',
    'Finance: save report filter packs locally and preview the first rows before exporting CSV.',
];

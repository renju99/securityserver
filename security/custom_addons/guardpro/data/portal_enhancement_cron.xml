<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- ========================================== -->
        <!-- Cron Job: Cleanup Stale Guard Locations   -->
        <!-- ========================================== -->
        
        <record id="ir_cron_cleanup_stale_guard_locations" model="ir.cron">
            <field name="name">GuardLink: Cleanup Stale Guard Locations</field>
            <field name="model_id" ref="model_guard_location_live"/>
            <field name="state">code</field>
            <field name="code">model.cleanup_stale_locations()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>
        
    </data>
</odoo>


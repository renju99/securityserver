<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.berkeleyuae.guardpro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!--
        CAMERA is requested at runtime by:
          * ``WebChromeClient.onPermissionRequest`` when the Emirates ID
            scanner calls ``navigator.mediaDevices.getUserMedia({video:true})``
            on the visitor registration / incident / audit screens.
          * ``WebChromeClient.onShowFileChooser`` when the user taps
            ``<input type="file" accept="image/*">`` and the OS picker
            offers "Take photo" (Android routes that through the camera
            app which inherits the caller's permission on Android 10+).
        Without this declaration the runtime request is denied without
        even showing the system dialog, so no camera UI ever appears
        and the Android app-info screen hides "Camera" from the
        permissions list.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <!-- Mark camera as optional so the APK still installs on devices
         without a rear camera (tablets), falling back to file picker
         from gallery. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
        allowBackup=false: the app stores a live Odoo ``session_id`` cookie
        and a queue of offline GPS fixes on device. Enrolling those in ADB /
        cloud auto-backup would let a device-level attacker resurrect an
        authenticated session on a different phone. Set to false per the
        standard posture for apps holding bearer credentials.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".TwaLauncherActivity"
            android:label="Berkeley Guard Pro"
            android:exported="true"
            android:launchMode="singleTask">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                App-link filter for https://security.berkeleyuae.com/guardpro/*
                Must match the host used by TwaLauncherActivity.START_URL and
                LocationService.API_ORIGIN - otherwise notification deep-links,
                outbox links, and email CTAs bounce to Chrome instead of the app.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="security.berkeleyuae.com"
                    android:pathPrefix="/guardpro"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <service
            android:name=".LocationService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="location" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!--
            FileProvider used by TwaLauncherActivity to hand the
            system camera app a content:// URI for photo capture
            triggered from <input type="file" accept="image/*"
            capture>. The authority MUST match the one built in
            Kotlin code: ``$packageName.fileprovider``.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
